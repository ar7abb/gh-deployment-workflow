# GitHub Deployment Workflow

This project demonstrates a simple CI/CD workflow using GitHub Actions and GitHub Pages.

The website is deployed automatically when `index.html` changes on the `main` branch.
